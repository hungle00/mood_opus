<div class="max-w-6xl mx-auto mt-4 px-2">
  <p style="color: green"><%= notice %></p>

  <div class="flex justify-between">
    <h1>Boards</h1>
    <%= link_to "New board", new_board_path, class: "btn-indigo focus:shadow-outline" %>
  </div>

  <div class="grid md:grid-cols-4 grid-cols-2 gap-4 mt-4">
    <% @boards.each do |board| %>
      <%= link_to board, class: 'rounded-lg border border-indigo-500 bg-white shadow-sm space-y-2 aspect-[4/3]' do %>
        <p class="text-center text-xl font-bold leading-6 text-gray-800"><%= board.title %></p>
      <% end %>
    <% end %>
  </div>
  
<div>